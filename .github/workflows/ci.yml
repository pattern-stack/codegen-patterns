name: CI

on:
  # Every pull request, whatever its base. Stacked PRs target their parent
  # branch, not `main`, so a `branches: [main]` filter left every PR in a
  # stack without the required `test-all` check — which blocks an atomic
  # stack merge, since GitHub needs the check on every member.
  pull_request:
  push:
    branches: [main]
  # Lets a run be started for a branch whose PR predates this change (a
  # retarget alone does not fire `pull_request`).
  workflow_dispatch:

# PR runs cancel stale siblings; main pushes QUEUE instead — cancelling a
# main run could kill the publish job mid-upload. The publish job's
# already-on-npm check makes queued runs cheap no-ops.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    name: test-all
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.x

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Install dependencies
        run: just install

      - name: Run test suite
        run: just test-all

      - name: Upload test artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: test-gen-output
          path: test/gen/
          retention-days: 14
          if-no-files-found: ignore

  # Scaffold integration suite — needs Docker (it starts its own Postgres via
  # docker compose), so it is NOT in `just test-all` and gets its own job: a
  # Docker flake fails here instead of masking the main suite. `ubuntu-latest`
  # ships Docker + compose, so CI can run it (GATE-1, #599).
  integration:
    name: test-integration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.x

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Install dependencies
        run: just install

      - name: Run scaffold integration suite
        run: just test-integration

      # SEM-3's demonstration gate. It shares this job's Docker rather than
      # getting one of its own, because it is one testcontainers suite. Today it
      # SKIPS here, loudly: @pattern-stack/query-surface is unpublished and does
      # not load against drizzle-orm 1.0 (query-surface#40). Wired in now so it
      # starts gating the day the package is installable, instead of being a
      # local-only check nobody remembers (CLAUDE.md: a gate outside CI rots).
      - name: Run the semantic demonstration gate
        run: just test-semantic-integration

  # Publish to npm on every merge to main, gated on green tests. Runs the
  # tarball smoke (#190), then publishes the root + each opted-in workspace
  # package IFF its version isn't already on npm — so merges without a
  # version bump are fast no-ops, and a bump to any one package publishes
  # exactly that package (independent versioning, ADR-036 §8).
  #
  # Auth is npm trusted publishing (OIDC) — no NPM_TOKEN secret. Each package
  # must list this repo + ci.yml as a trusted publisher on npmjs.com.
  publish:
    name: publish
    runs-on: ubuntu-latest
    # Both suites: a gate that cannot block a publish is a gate that rots
    # (GATE-1, #599).
    needs: [test, integration]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    timeout-minutes: 20
    permissions:
      id-token: write # mint the OIDC token npm exchanges for publish credentials
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.x

      - name: Setup node
        uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Update npm (trusted publishing needs >= 11.5.1)
        run: npm install -g npm@latest

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Install dependencies
        run: just install

      - name: Publish (tarball smoke + publish-if-bumped)
        run: just publish-ci

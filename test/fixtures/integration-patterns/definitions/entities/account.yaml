# Snapshot fixture (RFC-0001 §7). Representative CRM entity. Refresh via
# `just refresh-integration-fixture` from a local integration-patterns checkout.
entity:
  name: account
  plural: accounts
  table: accounts
  surface: crm
  # RFC-0002 §4/§8: pattern: Integrated unlocks the integration assembly +
  # default sink emission (the projection/upsert path E2 wires).
  pattern: Integrated
fields:
  name:
    type: string
    required: true

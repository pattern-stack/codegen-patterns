// @generated by @pattern-stack/codegen from entities/person.yaml — DO NOT EDIT.
// Hand edits are overwritten on re-emit. Regenerate with `bun run codegen`.

import { createEntityHooks } from '@pattern-stack/frontend-patterns';
import { personCollection } from '../collections/person';
import { personApi } from '../api/person';
import { getSyncMode } from '../config';
import type { Person } from '@repo/db/entities/person';

/**
 * Typed hooks for Person, wired via the framework factory.
 *
 * `localFirst` is resolved at call time from the entity's runtime sync mode
 * (`getSyncMode('person')`) — `api` mode is confirmed-write, everything else
 * is local-first (optimistic).
 */
export const personHooks = createEntityHooks<Person>({
	name: 'person',
	collection: personCollection,
	api: personApi,
	localFirst: () => getSyncMode('person') !== 'api',
});

// Per-entity hook re-exports for direct imports.
export const {
	useList: usePersonList,
	useGet: usePerson,
	useCreate: useCreatePerson,
	useUpdate: useUpdatePerson,
	useDelete: useDeletePerson,
	keys: personKeys,
} = personHooks;

// @generated by @pattern-stack/codegen from entities/user.yaml — DO NOT EDIT.
// Hand edits are overwritten on re-emit. Regenerate with `bun run codegen`.

import { createEntityHooks } from '@pattern-stack/frontend-patterns';
import { userCollection } from '../collections/user';
import { userApi } from '../api/user';
import { getSyncMode } from '../config';
import type { User } from '@repo/db/entities/user';

/**
 * Typed hooks for User, wired via the framework factory.
 *
 * `localFirst` is resolved at call time from the entity's runtime sync mode
 * (`getSyncMode('user')`) — `api` mode is confirmed-write, everything else
 * is local-first (optimistic).
 */
export const userHooks = createEntityHooks<User>({
	name: 'user',
	collection: userCollection,
	api: userApi,
	localFirst: () => getSyncMode('user') !== 'api',
});

// Per-entity hook re-exports for direct imports.
export const {
	useList: useUserList,
	useGet: useUser,
	useCreate: useCreateUser,
	useUpdate: useUpdateUser,
	useDelete: useDeleteUser,
	keys: userKeys,
} = userHooks;

# Minimal parent entity — opportunity (Synced pattern)
entity:
  name: opportunity
  plural: opportunities
  table: opportunities
  pattern: Integrated

fields:
  name:
    type: string
    required: true
  amount:
    type: decimal
    nullable: true

behaviors:
  - timestamps

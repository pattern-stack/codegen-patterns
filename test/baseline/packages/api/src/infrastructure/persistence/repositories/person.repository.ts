/**
 * Person Repository Implementation
 * Generated by entity codegen - do not edit directly
 */

import { Inject, Injectable } from '@nestjs/common';
import { eq } from 'drizzle-orm';
import { DRIZZLE } from '../../../constants/tokens';
import type {
	CreatePersonInput,
	IPersonRepository,
	UpdatePersonInput,
} from '../../../domain';
import { Person } from '../../../domain';
import type { DrizzleDB } from '../database.module';
import { persons } from '@repo/db/server/schema';

@Injectable()
export class PersonRepository implements IPersonRepository {
	constructor(@Inject(DRIZZLE) private readonly db: DrizzleDB) {}

	async create(input: CreatePersonInput): Promise<Person> {
		const result = await this.db
			.insert(persons)
			.values({
				tenantId: input.tenantId,
				email: input.email,
				firstName: input.firstName,
				lastName: input.lastName,
			})
			.returning();

		const record = result[0] as typeof persons.$inferSelect;
		return Person.fromRecord(record);
	}

	async findById(id: string): Promise<Person | null> {
		const result = await this.baseQuery()
			.where(eq(persons.id, id))
			.limit(1);

		const record = result[0];
		return record ? Person.fromRecord(record) : null;
	}

	async findAll(): Promise<Person[]> {
		const records = await this.baseQuery();
		return records.map(Person.fromRecord);
	}

	async update(id: string, input: UpdatePersonInput): Promise<Person | null> {
		const result = await this.db
			.update(persons)
			.set({
				...input,
			})
			.where(eq(persons.id, id))
			.returning();

		const record = result[0];
		return record ? Person.fromRecord(record) : null;
	}

	async delete(id: string): Promise<Person | null> {
		const result = await this.db
			.delete(persons)
			.where(eq(persons.id, id))
			.returning();

		const record = result[0];
		return record ? Person.fromRecord(record) : null;
	}

	private baseQuery() {
		return this.db.select().from(persons);
	}
}

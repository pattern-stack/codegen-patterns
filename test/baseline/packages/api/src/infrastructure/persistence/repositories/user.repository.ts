/**
 * User Repository Implementation
 * Generated by entity codegen - do not edit directly
 */

import { Injectable } from '@nestjs/common';
import { eq } from 'drizzle-orm';
import { BaseRepository, type BehaviorConfig } from '../base.repository';
import type {
	CreateUserInput,
	IUserRepository,
	UpdateUserInput,
	UserWith,
} from '../../../domain';
import { User, Person, Opportunity } from '../../../domain';
import { users } from '../drizzle';

@Injectable()
export class UserRepository
	extends BaseRepository<
		typeof users,
		User,
		CreateUserInput,
		UpdateUserInput
	>
	implements IUserRepository
{
	protected readonly table = users;

	// Behaviors declared in YAML -> generated as config object
	protected readonly behaviors: BehaviorConfig = {
		timestamps: false,
		softDelete: false,
		userTracking: false,
	};

	protected toEntity(record: typeof users.$inferSelect): User {
		return User.fromRecord(record);
	}

	async findByPersonId(id: string): Promise<User[]> {
		const records = await this.baseQuery()
			.where(eq(this.table.personId, id));
		return records.map((r) => this.toEntity(r));
	}

	// ═══════════════════════════════════════════════════════════════════════
	// Relationship loading (extends base class)
	// ═══════════════════════════════════════════════════════════════════════

	async findByIdWithRelations(
		id: string,
		include?: UserWith,
	): Promise<User | null> {
		const record = await this.db.query.users.findFirst({
			where: eq(users.id, id),
			with: this.buildWithClause(include),
		});
		return record ? this.mapToEntityWithRelations(record) : null;
	}

	async findAllWithRelations(include?: UserWith): Promise<User[]> {
		const records = await this.db.query.users.findMany({
			with: this.buildWithClause(include),
		});
		return records.map((r) => this.mapToEntityWithRelations(r));
	}

	private buildWithClause(include?: UserWith) {
		if (!include) return undefined;
		const result: Record<string, true> = {};
		if (include.person) result.person = true;
		if (include.owned_opportunities) result.owned_opportunities = true;
		return Object.keys(result).length > 0 ? result : undefined;
	}

	// biome-ignore lint/suspicious/noExplicitAny: Drizzle relational query returns dynamic shape
	private mapToEntityWithRelations(record: any): User {
		return User.fromRecord(record, (name, data) => {
			switch (name) {
				case 'person':
					// biome-ignore lint/suspicious/noExplicitAny: Cast for Drizzle record
					return Person.fromRecord(data as any);
				case 'owned_opportunities':
					// biome-ignore lint/suspicious/noExplicitAny: Cast for Drizzle records
					return (data as any[]).map((r) => Opportunity.fromRecord(r));
				default:
					return data;
			}
		});
	}
}

/**
 * Person Drizzle Schema
 * Generated by entity codegen - do not edit directly
 */

import {
	pgTable,
	uuid,
	varchar,
	index,
} from 'drizzle-orm/pg-core';

export const persons = pgTable('persons', {
	id: uuid('id').defaultRandom().primaryKey(),
	tenantId: uuid('tenant_id').notNull(),
	email: varchar('email', { length: 255 }).notNull(),
	firstName: varchar('first_name', { length: 100 }).notNull(),
	lastName: varchar('last_name', { length: 100 }).notNull(),
}, (table) => ({
	tenantIdIdx: index('idx_persons_tenant_id').on(table.tenantId),
	emailIdx: index('idx_persons_email').on(table.email),
}));

export type PersonRecord = typeof persons.$inferSelect;
export type NewPersonRecord = typeof persons.$inferInsert;

// @generated by @pattern-stack/codegen from test/fixtures/entities/tenant.yaml — DO NOT EDIT. Hand edits are overwritten on re-emit. Regenerate with `bun run codegen`. To extend, use a pattern (src/patterns/*.pattern.ts) or the entity YAML.
import { Controller, Get, Post, Patch, Delete, Body, NotFoundException, Param, ParseUUIDPipe, Query } from '@nestjs/common';
import { ApiBearerAuth, ApiBody, ApiOperation, ApiParam, ApiQuery, ApiResponse } from '@nestjs/swagger';
import { ZodValidationPipe } from '@shared/pipes/zod-validation.pipe';
import type { Page } from '@shared/http/page';
import { ListTenantsQuerySchema } from './dto/list-tenants.query';
import type { ListTenantsQueryDto } from './dto/list-tenants.query';
import { FindTenantByIdUseCase } from './use-cases/find-tenant-by-id.use-case';
import { ListTenantsUseCase } from './use-cases/list-tenants.use-case';
import { CreateTenantUseCase } from './use-cases/create-tenant.use-case';
import { UpdateTenantUseCase } from './use-cases/update-tenant.use-case';
import { DeleteTenantUseCase } from './use-cases/delete-tenant.use-case';
import { CreateTenantSchema } from './dto/create-tenant.dto';
import type { CreateTenantDto } from './dto/create-tenant.dto';
import { UpdateTenantSchema } from './dto/update-tenant.dto';
import type { UpdateTenantDto } from './dto/update-tenant.dto';
import type { Tenant } from './tenant.entity';

// OPENAPI-3: decorators reference registered schemas by `$ref` because
// the DTOs are Zod-derived types (OPENAPI-2 registers them by name at
// onModuleInit). `ErrorResponseDto` is auto-registered by the shared
// registry.
@ApiBearerAuth()
@Controller('tenants')
export class TenantController {
  constructor(
    // All routes go through use cases (ADR-003 — no controller → service shortcuts)
    private readonly findByIdUseCase: FindTenantByIdUseCase,
    private readonly listUseCase: ListTenantsUseCase,
    private readonly createUseCase: CreateTenantUseCase,
    private readonly updateUseCase: UpdateTenantUseCase,
    private readonly deleteUseCase: DeleteTenantUseCase,
  ) {}

  @ApiOperation({ summary: 'List tenants', operationId: 'listTenants' })
  @ApiQuery({ name: 'page', required: false, type: 'integer', description: '1-based page number (default 1).' })
  @ApiQuery({ name: 'pageSize', required: false, type: 'integer', description: 'Page size (default 50, max 200).' })
  @ApiQuery({ name: 'cursor', required: false, type: 'string', description: 'Opaque keyset cursor (accepted; v1 paginates by offset).' })
  @ApiQuery({ name: 'sort_by', required: false, type: 'string', description: "Sort column (default 'created_at')." })
  @ApiQuery({ name: 'sort_order', required: false, enum: ['asc', 'desc'], description: "Sort direction (default 'desc')." })
  @ApiResponse({
    status: 200,
    schema: {
      type: 'object',
      properties: {
        items: { type: 'array', items: { $ref: '#/components/schemas/TenantOutputDto' } },
        page: { type: 'integer' },
        pageCount: { type: 'integer' },
        total: { type: 'integer' },
        pageSize: { type: 'integer' },
        nextCursor: { type: 'string', nullable: true },
      },
      required: ['items', 'page', 'pageCount', 'total', 'pageSize', 'nextCursor'],
    },
  })
  @ApiResponse({ status: 401, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @Get()
  async getAll(
    @Query(new ZodValidationPipe(ListTenantsQuerySchema)) query: ListTenantsQueryDto,
  ): Promise<Page<Tenant>> {
    return this.listUseCase.execute(query);
  }

  @ApiOperation({ summary: 'Find tenant by id', operationId: 'findTenantById' })
  @ApiResponse({ status: 200, schema: { $ref: '#/components/schemas/TenantOutputDto' } })
  @ApiResponse({ status: 401, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiResponse({ status: 404, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiParam({ name: 'id', type: 'string', format: 'uuid' })
  @Get(':id')
  async getById(@Param('id', ParseUUIDPipe) id: string): Promise<Tenant> {
    // Use case throws NotFoundException on null/undefined (D2)
    return this.findByIdUseCase.execute(id);
  }


  @ApiOperation({ summary: 'Create tenant', operationId: 'createTenant' })
  @ApiBody({ schema: { $ref: '#/components/schemas/CreateTenantDto' } })
  @ApiResponse({ status: 201, schema: { $ref: '#/components/schemas/TenantOutputDto' } })
  @ApiResponse({ status: 400, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiResponse({ status: 401, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @Post()
  async create(
    @Body(new ZodValidationPipe(CreateTenantSchema)) dto: CreateTenantDto,
  ): Promise<Tenant> {
    return this.createUseCase.execute(dto);
  }

  @ApiOperation({ summary: 'Update tenant', operationId: 'updateTenant' })
  @ApiBody({ schema: { $ref: '#/components/schemas/UpdateTenantDto' } })
  @ApiResponse({ status: 200, schema: { $ref: '#/components/schemas/TenantOutputDto' } })
  @ApiResponse({ status: 400, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiResponse({ status: 401, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiResponse({ status: 404, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiParam({ name: 'id', type: 'string', format: 'uuid' })
  @Patch(':id')
  async update(
    @Param('id', ParseUUIDPipe) id: string,
    @Body(new ZodValidationPipe(UpdateTenantSchema)) dto: UpdateTenantDto,
  ): Promise<Tenant> {
    const entity = await this.updateUseCase.execute(id, dto);
    if (!entity) throw new NotFoundException(`Tenant ${id} not found`);
    return entity;
  }

  @ApiOperation({ summary: 'Delete tenant', operationId: 'deleteTenant' })
  @ApiResponse({ status: 204 })
  @ApiResponse({ status: 401, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiResponse({ status: 404, schema: { $ref: '#/components/schemas/ErrorResponseDto' } })
  @ApiParam({ name: 'id', type: 'string', format: 'uuid' })
  @Delete(':id')
  async remove(
    @Param('id', ParseUUIDPipe) id: string,
  ): Promise<void> {
    return this.deleteUseCase.execute(id);
  }

}

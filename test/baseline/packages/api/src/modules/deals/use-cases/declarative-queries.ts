// @generated by @pattern-stack/codegen from test/fixtures/entities/demo-deal.yaml — DO NOT EDIT. Hand edits are overwritten on re-emit. Regenerate with `bun run codegen`. To extend, use a pattern (src/patterns/*.pattern.ts) or the entity YAML.
/**
 * Declarative Query Use Cases for Deal
 * Generated from queries: block in entity YAML — do not edit directly.
 *
 * Each query is an injectable use case class that delegates to the service.
 * Register all via `declarativeQueryClasses` in the module providers.
 */

import { Injectable } from '@nestjs/common';
import { DealService } from '../deal.service';
import type { Deal } from '../deal.entity';

@Injectable()
export class FindDealByOwnerIdUseCase {
  constructor(private readonly service: DealService) {}

  async execute(ownerId: string): Promise<Deal[]> {
    return this.service.findByOwnerId(ownerId);
  }
}

@Injectable()
export class FindDealByAccountIdUseCase {
  constructor(private readonly service: DealService) {}

  async execute(accountId: string): Promise<Deal[]> {
    return this.service.findByAccountId(accountId);
  }
}

@Injectable()
export class FindDealByStageUseCase {
  constructor(private readonly service: DealService) {}

  async execute(stage: 'prospecting' | 'qualification' | 'proposal' | 'negotiation' | 'closed_won' | 'closed_lost'): Promise<Deal[]> {
    return this.service.findByStage(stage);
  }
}

@Injectable()
export class FindDealByOwnerIdAndStageUseCase {
  constructor(private readonly service: DealService) {}

  async execute(ownerId: string, stage: 'prospecting' | 'qualification' | 'proposal' | 'negotiation' | 'closed_won' | 'closed_lost'): Promise<Deal[]> {
    return this.service.findByOwnerIdAndStage(ownerId, stage);
  }
}

export const declarativeQueryClasses = [
  FindDealByOwnerIdUseCase,
  FindDealByAccountIdUseCase,
  FindDealByStageUseCase,
  FindDealByOwnerIdAndStageUseCase,
];

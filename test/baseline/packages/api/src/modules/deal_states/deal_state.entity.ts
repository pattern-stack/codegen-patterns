// @generated by @pattern-stack/codegen from test/fixtures/entities/deal_state.yaml — DO NOT EDIT. Hand edits are overwritten on re-emit. Regenerate with `bun run codegen`. To extend, use a pattern (src/patterns/*.pattern.ts) or the entity YAML.
import {
  index,
  integer,
  pgEnum,
  pgTable,
  text,
  uuid,
  type AnyPgColumn,
} from 'drizzle-orm/pg-core';
import { type InferSelectModel } from 'drizzle-orm';
import { tenants } from '../tenants/tenant.entity';

export const dealStateStateTypeEnum = pgEnum('deal_state_state_type', ['open', 'won', 'lost', 'stalled']);

export const deal_states = pgTable(
  'deal_states',
  {
    id: uuid('id').primaryKey().defaultRandom(),
    tenantId: uuid('tenant_id').references((): AnyPgColumn => tenants.id),
    name: text('name').notNull(),
    displayName: text('display_name').notNull(),
    stateType: dealStateStateTypeEnum('state_type').notNull(),
    sortOrder: integer('sort_order').notNull(),
    probabilityDefault: integer('probability_default'),
    color: text('color'),
  },
  (t) => [
    index('deal_states_tenant_id_idx').on(t.tenantId),
  ],
);

export type DealState = InferSelectModel<typeof deal_states>;
export type DealStateInsert = typeof deal_states.$inferInsert;

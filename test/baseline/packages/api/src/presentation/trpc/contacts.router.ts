// @generated by @pattern-stack/codegen from test/fixtures/contact-v2.yaml — DO NOT EDIT. Hand edits are overwritten on re-emit. Regenerate with `bun run codegen`. To extend, use the entity YAML or a base-class behavior config.
/**
 * Contacts tRPC Router
 * Generated by entity codegen - do not edit directly
 *
 * Add this router to TrpcModule providers:
 *   providers: [..., ContactsRouter],
 */

import { Injectable } from '@nestjs/common';
import { Router, Query, Mutation, Input } from '@mguay/nestjs-trpc';
import { z } from 'zod';
import {
	createContactSchema,
	updateContactSchema,
} from '../../application/schemas';
import { GetContactByIdQuery } from '../../application/queries/contact/get-by-id.query';
import { ListContactsQuery } from '../../application/queries/contact/list.query';
import { CreateContactCommand } from '../../application/commands/contact/create.command';
import { UpdateContactCommand } from '../../application/commands/contact/update.command';
import { DeleteContactCommand } from '../../application/commands/contact/delete.command';

@Injectable()
@Router({ alias: 'contacts' })
export class ContactsRouter {
	constructor(
		private readonly getContactById: GetContactByIdQuery,
		private readonly listContacts: ListContactsQuery,
		private readonly createContact: CreateContactCommand,
		private readonly updateContact: UpdateContactCommand,
		private readonly deleteContact: DeleteContactCommand,
	) {}

	@Query()
	async list() {
		return this.listContacts.execute();
	}

	@Query({ input: z.object({ id: z.string().uuid() }) })
	async getById(@Input('id') id: string) {
		return this.getContactById.execute(id);
	}

	@Mutation({ input: createContactSchema })
	async create(@Input() input: z.infer<typeof createContactSchema>) {
		return this.createContact.execute(input);
	}

	@Mutation({ input: z.object({ id: z.string().uuid(), data: updateContactSchema }) })
	async update(
		@Input('id') id: string,
		@Input('data') data: z.infer<typeof updateContactSchema>,
	) {
		return this.updateContact.execute(id, data);
	}

	@Mutation({ input: z.object({ id: z.string().uuid() }) })
	async delete(@Input('id') id: string) {
		return this.deleteContact.execute(id);
	}
}

/**
 * Relationship Composition Queries for DealState
 * Generated by entity codegen - do not edit directly
 *
 * These query classes implement service-layer composition via FK + single-table
 * repo calls. No SQL JOINs. Two queries, no JOIN. (CGP-358 / CGP-62)
 */

import { Inject, Injectable } from '@nestjs/common';
import { DEALSTATE_REPOSITORY } from '../../../constants';
import type { IDealStateRepository } from '../../../domain/deal_state';
import type { DealState } from '../../../domain/deal_state';
import type { IOpportunityRepository } from '../../../domain/opportunity';
import type { Opportunity } from '../../../domain/opportunity';

/**
 * Resolves the opportunities (Opportunity[]) for a given DealState.
 * Single repo call with FK filter + optional cursor/limit pagination.
 */
@Injectable()
export class DealStateOpportunitiesQuery {
	constructor(
		@Inject('OPPORTUNITY_REPOSITORY')
		private readonly opportunityRepo: IOpportunityRepository,
	) {}

	async execute(dealStateId: string, opts?: { cursor?: string; limit?: number }): Promise<Opportunity[]> {
		return this.opportunityRepo.findByStateId(dealStateId, opts);
	}
}

export const relationshipQueryClasses = [
	DealStateOpportunitiesQuery,
];

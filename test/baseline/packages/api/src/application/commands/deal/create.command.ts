// @generated by @pattern-stack/codegen from test/fixtures/demo-deal.yaml — DO NOT EDIT. Hand edits are overwritten on re-emit. Regenerate with `bun run codegen`. To extend, use the entity YAML or a base-class behavior config.
/**
 * Create Deal Command
 * Generated by entity codegen - do not edit directly
 *
 * EXTENSION POINTS (add business logic here):
 * - Validation: Add domain validation rules before creating
 * - Defaults: Compute default values not handled by DTO
 * - Side effects: Emit events, send notifications, create related entities
 * - Authorization: Check user permissions beyond route-level auth
 * - Audit: Log creation for compliance/debugging
 */

import { Inject, Injectable } from '@nestjs/common';
import { DEAL_REPOSITORY } from '../../../constants';
import type { CreateDealInput, IDealRepository } from '../../../domain';
import { Deal } from '../../../domain';
import type { CreateDealDto } from '../../schemas';

@Injectable()
export class CreateDealCommand {
	constructor(
		@Inject(DEAL_REPOSITORY)
		private readonly dealRepository: IDealRepository,
	) {}

	async execute(
		dto: CreateDealDto,
	): Promise<Deal> {
		// TODO: Add pre-create validation and business rules here

		// Map DTO to domain input
		const input: CreateDealInput = {
			name: dto.name,
			amount: dto.amount ?? null,
			stage: dto.stage,
			ownerId: dto.ownerId,
			accountId: dto.accountId,
			closeDate: dto.closeDate ?? null,
			probability: dto.probability ?? null,
		};

		const created = await this.dealRepository.create(input);

		// TODO: Add post-create side effects here (events, notifications, etc.)

		return created;
	}
}

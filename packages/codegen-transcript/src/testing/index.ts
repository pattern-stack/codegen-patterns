/**
 * @pattern-stack/codegen-transcript/testing — test-time helpers.
 *
 * Conformance checks for transcript adapter implementations, kept out of the
 * main runtime barrel so production bundles don't pull in test scaffolding.
 */

export * from './assert-transcript-adapter';

/**
 * @pattern-stack/codegen-mail/testing — test-time helpers.
 *
 * Conformance checks for mail adapter implementations, kept out of the main
 * runtime barrel so production bundles don't pull in test scaffolding.
 */

export * from './assert-mail-adapter';

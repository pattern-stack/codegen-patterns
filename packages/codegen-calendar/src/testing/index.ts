/**
 * @pattern-stack/codegen-calendar/testing — test-time helpers.
 *
 * Conformance checks for calendar adapter implementations, kept out of the main
 * runtime barrel so production bundles don't pull in test scaffolding.
 */

export * from './assert-calendar-adapter';
